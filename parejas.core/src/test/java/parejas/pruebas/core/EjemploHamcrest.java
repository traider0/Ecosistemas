package parejas.pruebas.core;

import static org.junit.Assert.*;
import static org.hamcrest.Matchers.*;

import org.junit.Test;

public class EjemploHamcrest {

	@Test
	public void test() {
		/*String color="rojo";
		assertEquals(true, "Fernando".startsWith("F"));
		assertThat("Fernando", 
				anyOf(
					startsWith("F"),
					endsWith("o"),
					containsString("rna")));
		
		assertThat(15, allOf(greaterThan(10),lessThan(100)));
		
		assertThat("rojo",is(color));*/
	}

}
